Multiple exports are possible from a single file by making module.exports an object containing many properties (each property an exportable object).

Note that if any function calls are made in the required file, they will run when the file that does the requireing is run.
